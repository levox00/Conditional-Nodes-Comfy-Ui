# pyproject.toml
[project]
name = "conditional-nodes-by-mikudes" # Unique identifier for your node. Immutable after creation.
description = "Logic, conditional and dynamic loader nodes for ComfyUI."
version = "1.0.0" # Custom Node version. Must be semantically versioned.
license = { file = "LICENSE" }
dependencies  = [] # Filled in from requirements.txt

[project.urls]
Repository = "https://github.com/levox00/Conditional-Nodes-Comfy-Ui"

[tool.comfy]
PublisherId = "mikudes" # TODO (fill in Publisher ID from Comfy Registry Website).
DisplayName = "Conditional Nodes by Mikudes" # Display name for the Custom Node. Can be changed later.
Icon = "https://example.com/icon.png" # SVG, PNG, JPG or GIF (MAX. 800x400px)